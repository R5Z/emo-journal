import { View, Text } from 'react-native';

export default function EditorModal() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Editor (WIP)</Text>
    </View>
  );
}
