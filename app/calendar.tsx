import { View, Text } from 'react-native';

export default function CalendarModal() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Calendar (WIP)</Text>
    </View>
  );
}
